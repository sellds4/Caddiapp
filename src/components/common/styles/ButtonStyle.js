var React = require('react-native');

var ButtonStyle = React.StyleSheet.create({
    blueButton: {
        backgroundColor: 'blue',
        borderRadius: 5,
        paddingVertical: 10,
        position: 'relative'
    },
    blueButtonText: {
        color: 'white',
        textAlign: 'center'
    }
});

module.exports = ButtonStyle;
