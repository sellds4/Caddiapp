var React = require('react-native');

var TabBarStyle = React.StyleSheet.create({
    tabBarContainer: {
        flexDirection: 'row',
        justifyContent: 'space-around'
    }
});

module.exports = TabBarStyle;